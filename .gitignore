# Configure .gitignore as a whitelist
# Ignore everything
*

# Ignore specific file types
.ipynb_checkpoints
*checkpoint.ipynb

# Skip directories, so we can recurse into them
!*/
!/.gitignore

# Whitelist
!tools/**
!graphs/databricks*
!datasets/databricks*
!datasets/bulk_databricks*
!!topics_template.ipynb
!/README.md

# SSH SigKey
